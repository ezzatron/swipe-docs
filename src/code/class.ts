import { cssClass } from "impasto";

const BASE = "imp";

export const LINE_NUMBERS_SHOW_CLASS = `${cssClass.lineNumbers}-s`;

export const SECTION_CONTENT_CLASS = `${BASE}-sc`;
export const SECTION_CONTENT_INDENT_CLASS = `${SECTION_CONTENT_CLASS}-i`;
export const SECTION_CONTEXT_CLASS = `${BASE}-sx`;
